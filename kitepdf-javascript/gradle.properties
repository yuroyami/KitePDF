POM_NAME=KitePDF JavaScript
POM_DESCRIPTION=Runs the JavaScript that PDF documents carry, on the KiteJS engine: document-level scripts and JavaScript actions, under an instruction budget so a script cannot hang the app. Every other KitePDF module stays free of any script engine.
