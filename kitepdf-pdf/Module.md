# Module kitepdf-pdf

The PDF engine: parse, render, extract, fill, edit and write PDF documents.

`PdfDocument` and `PdfPage`, the content-stream interpreter, text extraction and
search, form filling with appearance synthesis, RC4 and AES encryption,
annotations (read-only), redaction, and the `PdfBuilder` and `PdfEditor` writers.
